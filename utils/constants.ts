export enum STATUS {
    ACTIVE = 1,
    INACTIVE = 2
}

export enum POPULAR {
    ACTIVE = 1,
    INACTIVE = 2
}

export enum FORM {
    TEXT = 1,
    DROPDOWN = 2,
    TEXTAREA = 3
}

export enum TOAST {
    SUCCESS = 'success',
    ERROR = 'error'
}

export enum PAGINATOR {
    SIZE = 10
}
